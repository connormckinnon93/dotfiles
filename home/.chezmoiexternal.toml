# Antidote - the ZSH plugin manager.
# Cloned (not a binary) into $ZDOTDIR/.antidote; sourced from .zshrc via `antidote load`.
# $ZDOTDIR is ~/.config/zsh (see .zshenv).
[".config/zsh/.antidote"]
    type = "git-repo"
    url = "https://github.com/mattmc3/antidote.git"
    refreshPeriod = "168h"
